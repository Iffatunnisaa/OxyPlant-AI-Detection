<!-- Navigation -->
<nav class="home-nav fixed top-0 inset-x-0 z-50 py-4 px-6 transition-all duration-300" id="main-nav">
  <div class="max-w-6xl mx-auto flex justify-between items-center">
    <!-- Logo -->
    <div class="text-xl md:text-2xl font-bold nav-brand">
      <a href="{{ route('home') }}" class="hover:no-underline">OxyPlant</a>
    </div>

    <!-- Desktop Menu -->
    <div class="hidden md:flex space-x-4 lg:space-x-6 items-center">
      @if(isAdmin)
        <a
          href="{{ route('admin.dashboard') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Admin Dashboard</a>
        <a
          href="{{ route('admin.plant_info.index') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Kelola Info Tanaman</a>
      @elseif(isAuthenticated)
        <a
          href="{{ route('garden_manager.index') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Garden Manager</a>
        <a
          href="{{ route('plant_info.index') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Info Tanaman</a>
      @endif
      @if(isAuthenticated && !isAdmin)
        <a
          href="{{ route('community.index') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Komunitas</a>
        <a
          href="{{ route('about') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Tentang</a>
      @endif
      @if(!isAuthenticated)
        <a
          href="{{ route('about') }}"
          class="nav-link hover:text-green-300 transition-colors duration-300 text-sm md:text-base"
        >Tentang</a>
      @endif
      @if(isAuthenticated)
        <a
          href="{{ route('profile') }}"
          class="nav-profile text-[#F1F0E9] hover:text-green-300 px-3 py-1 md:px-4 md:py-2 rounded-lg ml-2 md:ml-4 transition-colors duration-300"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-6 w-6"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
            />
          </svg>
        </a>
      @else
        <a
          href="{{ route('auth.login') }}"
          class="inline-flex items-center justify-center bg-yellow-400 hover:bg-yellow-300 text-[#0f2c21] font-semibold px-4 py-2 rounded-lg ml-2 md:ml-4 text-sm md:text-base transition-colors duration-300"
        >
          Login
        </a>
      @endif
    </div>

    <!-- Mobile Menu Button -->
    <button
      id="mobile-menu-button"
      class="md:hidden nav-mobile-button focus:outline-none transition-colors duration-300"
      onclick="toggleMobileMenu()"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />

      </svg>
    </button>
  </div>

  <!-- Mobile Menu (hidden by default) -->
  <div id="mobile-menu" class="hidden md:hidden pt-4 pb-2">
    @if(isAdmin)
      <a
        href="{{ route('admin.dashboard') }}"
        class="block nav-link hover:text-green-300 py-2 px-4"
      >Admin Dashboard</a>
      <a
        href="{{ route('admin.plant_info.index') }}"
        class="block nav-link hover:text-green-300 py-2 px-4"
      >Kelola Info Tanaman</a>
    @elseif(isAuthenticated)
      <a
        href="{{ route('garden_manager.index') }}"
        class="block nav-link hover:text-green-300 py-2 px-4"
      >Garden Manager</a>
      <a
        href="{{ route('plant_info.index') }}"
        class="block nav-link hover:text-green-300 py-2 px-4"
      >Info Tanaman</a>
    @endif
    @if(isAuthenticated && !isAdmin)
      <a
        href="{{ route('community.index') }}"
        class="block nav-link hover:text-green-300 py-2 px-4"
      >Komunitas</a>
      <a href="{{ route('about') }}" class="block nav-link hover:text-green-300 py-2 px-4">Tentang</a>
    @endif
    @if(!isAuthenticated)
      <a href="{{ route('about') }}" class="block nav-link hover:text-green-300 py-2 px-4">Tentang</a>
    @endif
    @if(isAuthenticated)
      <a
        href="{{ route('profile') }}"
        class="block bg-green-600 hover:bg-green-700 text-[#F1F0E9] py-2 px-4 rounded-lg mt-2 text-center"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-6 w-6"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
          />
        </svg>
      </a>
    @else
      <a
        href="{{ route('auth.login') }}"
        class="block bg-yellow-400 hover:bg-yellow-300 text-[#0f2c21] font-semibold py-2 px-4 rounded-lg mt-2 text-center transition-colors duration-300"
      >
        Login
      </a>
    @endif
  </div>
</nav>
