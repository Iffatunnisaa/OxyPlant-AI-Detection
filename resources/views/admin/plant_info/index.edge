@layout.app({ title: "Kelola Informasi Tanaman | OxyPlant" })

@slot('main')
  <main class="bg-[#f2f1eb]">
    <section class="mx-auto max-w-6xl px-4 py-10 sm:px-6 lg:px-8 lg:py-14">
      <!-- Header -->
      <div class="mb-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
        <div>
          <h1 class="text-3xl font-bold text-[#0f4a34] lg:text-4xl">Kelola Informasi Tanaman</h1>
          <p class="mt-2 text-sm text-[#5e6d63]">Tambah, edit, atau hapus data tanaman yang digunakan di fitur Info Tanaman.</p>
        </div>
        <a href="{{ route('admin.plant_info.create') }}" 
           class="inline-flex items-center justify-center gap-2 rounded-xl bg-[#14573e] px-6 py-3 text-sm font-semibold text-white shadow-sm transition duration-200 hover:bg-[#0f4a34] hover:shadow-md">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
          Add Plant
        </a>
      </div>

      <!-- Status Messages -->
      @if(error)
        <div class="mb-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-red-700">
          {{ error }}
        </div>
      @endif

      <!-- Plants Table / Cards -->
      @if(plants.length === 0)
        <!-- Empty State -->
        <div class="rounded-3xl border border-[#dbe3d7] bg-white p-12 text-center shadow-sm">
          <div class="mx-auto h-16 w-16 rounded-full bg-[#f8faf5] flex items-center justify-center">
            <svg class="h-8 w-8 text-[#6a7b70]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
          </div>
          <h3 class="mt-4 text-lg font-semibold text-[#0f4a34]">No plants yet</h3>
          <p class="mt-1 text-sm text-[#5e6d63]">Start by adding your first plant to the database.</p>
          <a href="{{ route('admin.plant_info.create') }}" 
             class="mt-4 inline-flex items-center gap-2 rounded-lg bg-[#3FAB5E]/10 px-4 py-2 text-sm font-semibold text-[#14573e] hover:bg-[#3FAB5E]/20">
            Add Plant
          </a>
        </div>
      @else
        <!-- Desktop Table View -->
        <div class="hidden overflow-hidden rounded-3xl border border-[#dbe3d7] bg-white shadow-sm lg:block">
          <table class="w-full">
            <thead>
              <tr class="border-b border-[#dbe3d7] bg-[#f8faf5]">
                <th class="px-6 py-4 text-left text-xs font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Plant Name</th>
                <th class="px-6 py-4 text-left text-xs font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Category</th>
                <th class="px-6 py-4 text-left text-xs font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Description</th>
                <th class="px-6 py-4 text-center text-xs font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Diseases</th>
                <th class="px-6 py-4 text-center text-xs font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Care Items</th>
                <th class="px-6 py-4 text-center text-xs font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Actions</th>
              </tr>
            </thead>
            <tbody>
              @each(plant in plants)
                <tr class="border-b border-[#dbe3d7] transition duration-200 hover:bg-[#f8faf5]">
                  <td class="px-6 py-4">
                    <div class="flex items-center gap-3">
                      @if(plant.image)
                        <img src="{{ plant.image }}" alt="{{ plant.name }}" class="h-10 w-10 rounded-lg object-cover" />
                      @else
                        <div class="h-10 w-10 rounded-lg bg-[#f8faf5]"></div>
                      @endif
                      <div>
                        <p class="font-semibold text-[#0f4a34]">{{ plant.name }}</p>
                        <p class="text-xs text-[#6a7b70]">{{ plant.shortName }}</p>
                      </div>
                    </div>
                  </td>
                  <td class="px-6 py-4 text-sm text-[#4d6056]">{{ plant.category }}</td>
                  <td class="px-6 py-4 text-sm text-[#4d6056]">
                    <p class="line-clamp-2">{{ plant.description }}</p>
                  </td>
                  <td class="px-6 py-4 text-center">
                    <span class="inline-block rounded-full bg-[#fdeceb] px-3 py-1 text-xs font-semibold text-[#a33a3a]">
                      {{ plant.diseases.length }}
                    </span>
                  </td>
                  <td class="px-6 py-4 text-center">
                    <span class="inline-block rounded-full bg-[#edf5ea] px-3 py-1 text-xs font-semibold text-[#14573e]">
                      {{ plant.care.length }}
                    </span>
                  </td>
                  <td class="px-6 py-4 text-center">
                    <div class="flex items-center justify-center gap-2">
                      <a href="{{ route('admin.plant_info.edit', { id: plant._id }) }}" 
                         class="inline-flex items-center gap-1 rounded-lg bg-blue-100 px-3 py-2 text-sm font-semibold text-blue-700 transition duration-200 hover:bg-blue-200">
                        Edit
                      </a>
                      <button type="button" 
                              data-delete-btn 
                              data-id="{{ plant._id }}"
                              data-delete-url="{{ route('admin.plant_info.destroy', { id: plant._id }) }}"
                              class="inline-flex items-center gap-1 rounded-lg bg-red-100 px-3 py-2 text-sm font-semibold text-red-700 transition duration-200 hover:bg-red-200">
                        Delete
                      </button>
                    </div>
                  </td>
                </tr>
              @endeach
            </tbody>
          </table>
        </div>

        <!-- Mobile Card View -->
        <div class="space-y-4 lg:hidden">
          @each(plant in plants)
            <div class="overflow-hidden rounded-2xl border border-[#dbe3d7] bg-white shadow-sm">
              <div class="p-4">
                <div class="flex items-start justify-between gap-3">
                  <div class="flex items-start gap-3">
                    @if(plant.image)
                      <img src="{{ plant.image }}" alt="{{ plant.name }}" class="h-12 w-12 rounded-lg object-cover" />
                    @else
                      <div class="h-12 w-12 rounded-lg bg-[#f8faf5]"></div>
                    @endif
                    <div>
                      <p class="font-semibold text-[#0f4a34]">{{ plant.name }}</p>
                      <p class="text-xs text-[#6a7b70]">{{ plant.category }}</p>
                    </div>
                  </div>
                </div>

                <p class="mt-3 line-clamp-2 text-sm text-[#4d6056]">{{ plant.description }}</p>

                <div class="mt-3 flex gap-3">
                  <div class="flex-1 rounded-lg bg-[#fdeceb] px-3 py-2 text-center">
                    <p class="text-xs text-[#6a7b70]">Diseases</p>
                    <p class="text-lg font-semibold text-[#a33a3a]">{{ plant.diseases.length }}</p>
                  </div>
                  <div class="flex-1 rounded-lg bg-[#edf5ea] px-3 py-2 text-center">
                    <p class="text-xs text-[#6a7b70]">Care Items</p>
                    <p class="text-lg font-semibold text-[#14573e]">{{ plant.care.length }}</p>
                  </div>
                </div>

                <div class="mt-4 flex gap-2">
                  <a href="{{ route('admin.plant_info.edit', { id: plant._id }) }}" 
                     class="flex-1 rounded-lg bg-blue-100 py-2 text-center text-sm font-semibold text-blue-700 transition duration-200 hover:bg-blue-200">
                    Edit
                  </a>
                  <button type="button" 
                          data-delete-btn 
                          data-id="{{ plant._id }}"
                          data-delete-url="{{ route('admin.plant_info.destroy', { id: plant._id }) }}"
                          class="flex-1 rounded-lg bg-red-100 py-2 text-center text-sm font-semibold text-red-700 transition duration-200 hover:bg-red-200">
                    Delete
                  </button>
                </div>
              </div>
            </div>
          @endeach
        </div>
      @endif
    </section>
  </main>
@endslot

@slot('script')
  <script>
    document.querySelectorAll('[data-delete-btn]').forEach(btn => {
      btn.addEventListener('click', async (e) => {
        e.preventDefault()
        const plantId = btn.dataset.id
        
        if (!confirm('Are you sure you want to delete this plant? This action cannot be undone.')) {
          return
        }

        try {
          const response = await fetch(btn.dataset.deleteUrl || '', {
            method: 'DELETE',
            headers: {
              'X-CSRF-TOKEN': document.querySelector('[name="csrf_token"]')?.value || '',
            }
          })

          const data = await response.json()

          if (data.success) {
            alert(data.message)
            location.reload()
          } else {
            alert('Error: ' + (data.message || 'Failed to delete plant'))
          }
        } catch (error) {
          alert('Error: ' + error.message)
        }
      })
    })
  </script>
@endslot
@end
