@layout.app({ title: "User Profile | OxyPlant" })
  @slot('main')
    <main class="bg-[#f2f1eb]">
      <section class="mx-auto max-w-5xl px-4 py-10 sm:px-6 lg:px-8 lg:py-14">
        <div class="overflow-hidden rounded-3xl border border-[#dbe3d7] bg-white shadow-sm">
          <div class="bg-linear-to-br from-[#edf5ea] via-white to-[#f7faf5] px-6 py-8 sm:px-8 sm:py-10 lg:px-10">
            <div class="flex flex-col gap-6 sm:flex-row sm:items-center sm:justify-between">
              <div class="flex flex-col gap-5 sm:flex-row sm:items-center">
                <div class="relative mx-auto h-28 w-28 shrink-0 sm:mx-0 sm:h-32 sm:w-32">
                  <div class="absolute inset-0 rounded-full bg-[#3FAB5E]/15 blur-2xl"></div>
                  <div class="relative h-full w-full overflow-hidden rounded-full border-4 border-white bg-white shadow-md">
                    <img src="/resources/img/profil.jpeg" alt="{{ user.fullname || 'User profile' }}" class="h-full w-full object-cover" />
                  </div>
                </div>

                <div class="text-center sm:text-left">
                  <span class="inline-flex items-center rounded-full border border-[#cfe0d0] bg-white/90 px-3 py-1 text-xs font-semibold uppercase tracking-[0.18em] text-[#14573e] shadow-sm">
                    Garden Owner
                  </span>
                  <h1 class="mt-4 text-3xl font-bold tracking-tight text-[#0f4a34] sm:text-4xl">
                    {{ user.fullname || 'Pengguna OxyPlant' }}
                  </h1>
                  <p class="mt-3 text-sm leading-relaxed text-[#5e6d63] sm:text-base">
                    {{ user.email }}
                    @if(user.phone)
                      <span class="mx-2 text-[#b1b8b1]">•</span>
                      {{ user.phone }}
                    @endif
                  </p>
                  <p class="mt-2 text-sm text-gray-500">
                    Bergabung {{ new Date(user.createdAt).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) }}
                  </p>
                </div>
              </div>

              <div class="rounded-2xl border border-[#dbe3d7] bg-white px-4 py-4 shadow-sm sm:min-w-45">
                <p class="text-sm text-gray-500">Total plants</p>
                <p class="mt-1 text-3xl font-bold text-[#0f4a34]">{{ plants.length }}</p>
                <p class="mt-1 text-sm text-[#5e6d63]">Tanaman terhubung ke akun ini.</p>
              </div>
            </div>
          </div>

          <div class="border-t border-[#dbe3d7] px-6 py-7 sm:px-8 lg:px-10">
            <div class="grid gap-6 lg:grid-cols-[1.05fr_.95fr] lg:items-start">
              <section class="space-y-6">
                <div class="rounded-3xl border border-[#dbe3d7] bg-[#f8faf5] p-6 shadow-sm">
                  <h2 class="text-xl font-semibold text-[#0f4a34]">Informasi Profil</h2>
                  <p class="mt-1 text-sm text-gray-500">Informasi akun yang tersimpan saat ini.</p>

                  <dl class="mt-5 grid gap-4 sm:grid-cols-2">
                    <div class="rounded-2xl border border-[#dbe3d7] bg-white p-4">
                      <dt class="text-sm text-gray-500">Full name</dt>
                      <dd class="mt-2 text-base font-semibold text-gray-800">{{ user.fullname || '-' }}</dd>
                    </div>
                    <div class="rounded-2xl border border-[#dbe3d7] bg-white p-4">
                      <dt class="text-sm text-gray-500">Email</dt>
                      <dd class="mt-2 text-base font-semibold text-gray-800">{{ user.email || '-' }}</dd>
                    </div>
                  </dl>
                </div>

                <div class="rounded-3xl border border-[#dbe3d7] bg-white p-6 shadow-sm">
                  <div class="flex items-center justify-between gap-4">
                    <div>
                      <h2 class="text-xl font-semibold text-[#0f4a34]">Edit Profile</h2>
                      <p class="mt-1 text-sm text-gray-500">Klik untuk membuka form edit.</p>
                    </div>
                    <button
                      id="toggle-profile-form"
                      type="button"
                      class="inline-flex items-center rounded-xl bg-[#14573e] px-4 py-2.5 text-sm font-semibold text-white transition duration-200 hover:bg-[#0f4a34] hover:shadow-md"
                    >
                      Edit Profile
                    </button>
                  </div>

                  <form id="profile-form" class="mt-6 hidden space-y-4">
                    {{ csrfField() }}
                    <div>
                      <label for="profile-name" class="mb-1.5 block text-sm font-medium text-gray-500">Name</label>
                      <input
                        id="profile-name"
                        name="fullname"
                        type="text"
                        value="{{ user.fullname || '' }}"
                        class="w-full rounded-xl border border-[#dbe3d7] bg-white px-4 py-3 text-gray-800 outline-none transition duration-200 placeholder:text-gray-400 focus:border-[#3FAB5E] focus:ring-2 focus:ring-[#3FAB5E]/20"
                      />
                    </div>

                    <div>
                      <label for="profile-email" class="mb-1.5 block text-sm font-medium text-gray-500">Email</label>
                      <input
                        id="profile-email"
                        name="email"
                        type="email"
                        value="{{ user.email || '' }}"
                        class="w-full rounded-xl border border-[#dbe3d7] bg-white px-4 py-3 text-gray-800 outline-none transition duration-200 placeholder:text-gray-400 focus:border-[#3FAB5E] focus:ring-2 focus:ring-[#3FAB5E]/20"
                      />
                    </div>

                    <div>
                      <label for="profile-password" class="mb-1.5 block text-sm font-medium text-gray-500">Password</label>
                      <input
                        id="profile-password"
                        name="password"
                        type="password"
                        placeholder="Leave blank to keep current password"
                        class="w-full rounded-xl border border-[#dbe3d7] bg-white px-4 py-3 text-gray-800 outline-none transition duration-200 placeholder:text-gray-400 focus:border-[#3FAB5E] focus:ring-2 focus:ring-[#3FAB5E]/20"
                      />
                    </div>

                    <div id="form-status-message" class="hidden rounded-2xl border border-[#dbe3d7] bg-[#f8faf5] p-4">
                      <p id="status-text" class="text-sm font-semibold text-[#0f4a34]"></p>
                    </div>

                    <div class="flex flex-col gap-3 sm:flex-row">
                      <button id="save-profile-btn" type="submit" class="inline-flex flex-1 items-center justify-center rounded-xl bg-[#14573e] px-5 py-3 text-sm font-semibold text-white transition duration-200 hover:bg-[#0f4a34] hover:shadow-md disabled:opacity-50 disabled:cursor-not-allowed">
                        <span id="save-btn-text">Save Changes</span>
                        <i id="save-btn-spinner" class="fas fa-spinner fa-spin ml-2 hidden"></i>
                      </button>
                      <button id="cancel-profile-form" type="button" class="inline-flex flex-1 items-center justify-center rounded-xl border border-[#dbe3d7] bg-white px-5 py-3 text-sm font-semibold text-[#4d6056] transition duration-200 hover:bg-[#f8faf5] hover:shadow-sm">
                        Cancel
                      </button>
                    </div>
                  </form>
                </div>
              </section>

              <aside class="space-y-6">
                <section class="rounded-3xl border border-[#dbe3d7] bg-white p-6 shadow-sm">
                  <div class="flex items-start justify-between gap-4">
                    <div>
                      <h2 class="text-xl font-semibold text-[#0f4a34]">Recent Activity</h2>
                      <p class="mt-1 text-sm text-gray-500">Tiga aktivitas terbaru dari akun Anda.</p>
                    </div>
                    <a href="{{ route('community.index') }}" class="text-sm font-semibold text-[#14573e] transition duration-200 hover:text-[#0f4a34]">
                      View all activity →
                    </a>
                  </div>

                  <div class="mt-5 space-y-3">
                    @if(posts.length)
                      @each(post in posts.slice(0, 3))
                        <article class="rounded-2xl border border-[#dbe3d7] bg-[#f8faf5] p-4 transition duration-200 hover:shadow-sm">
                          <div class="flex items-start gap-3">
                            <div class="mt-0.5 flex h-10 w-10 shrink-0 items-center justify-center rounded-2xl bg-white text-[#14573e] shadow-sm">
                              @if(post.status == 'tumbuh')
                                <i class="fas fa-seedling"></i>
                              @elseif(post.status == 'siap panen')
                                <i class="fas fa-apple-alt text-[#b38c2d]"></i>
                              @elseif(post.status == 'terkena hama')
                                <i class="fas fa-bug text-[#c75b5b]"></i>
                              @else
                                <i class="fas fa-leaf"></i>
                              @endif
                            </div>
                            <div class="min-w-0 flex-1">
                              <div class="flex flex-wrap items-center gap-2">
                                <p class="font-semibold text-[#0f4a34]">{{ post.plant_id?.name || 'Tanaman' }}</p>
                                <span class="rounded-full border border-[#dbe3d7] bg-white px-2.5 py-1 text-[11px] font-semibold uppercase tracking-[0.14em] text-[#6a7b70]">
                                  {{ post.status }}
                                </span>
                              </div>
                              <p class="mt-1 text-sm text-gray-500">
                                {{ post.createdAt.toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) }}
                              </p>
                              <p class="mt-2 text-sm leading-relaxed text-[#5e6d63]">
                                {{ post.message }}
                              </p>
                            </div>
                          </div>
                        </article>
                      @endeach
                    @else
                      <div class="rounded-2xl border border-dashed border-[#cfd8cd] bg-[#f8faf5] px-5 py-7 text-center">
                        <p class="text-sm font-semibold text-[#0f4a34]">Belum ada aktivitas terbaru.</p>
                      </div>
                    @endif
                  </div>
                </section>

                <section class="rounded-3xl border border-[#dbe3d7] bg-white p-6 shadow-sm">
                  <h2 class="text-xl font-semibold text-[#0f4a34]">Account Actions</h2>
                  <p class="mt-1 text-sm text-gray-500">Aksi akun yang paling penting.</p>

                  <div class="mt-5 flex flex-col gap-3 sm:flex-row">
                    <a
                      href="{{ route('auth.logout') }}"
                      class="inline-flex flex-1 items-center justify-center rounded-xl border border-[#dbe3d7] bg-white px-5 py-3 text-sm font-semibold text-[#4d6056] transition duration-200 hover:bg-[#f8faf5] hover:shadow-sm"
                    >
                      <i class="fas fa-sign-out-alt mr-2"></i>
                      Keluar
                    </a>
                    <button
                      type="button"
                      class="inline-flex flex-1 items-center justify-center rounded-xl border border-[#f0d0cf] bg-[#fff5f5] px-5 py-3 text-sm font-semibold text-[#b2534e] transition duration-200 hover:bg-[#ffecec] hover:shadow-sm"
                    >
                      Hapus Akun
                    </button>
                  </div>
                </section>
              </aside>
            </div>
          </div>
        </div>
      </section>
    </main>
  @endslot

  @slot('script')
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        const toggleButton = document.getElementById('toggle-profile-form')
        const cancelButton = document.getElementById('cancel-profile-form')
        const saveButton = document.getElementById('save-profile-btn')
        const profileForm = document.getElementById('profile-form')
        const profileNameInput = document.getElementById('profile-name')
        const statusMessage = document.getElementById('form-status-message')
        const statusText = document.getElementById('status-text')
        const saveBtnText = document.getElementById('save-btn-text')
        const saveBtnSpinner = document.getElementById('save-btn-spinner')

        if (!toggleButton || !cancelButton || !profileForm) {
          console.warn('Profile form elements not found')
          return
        }

        const openForm = () => {
          profileForm.classList.remove('hidden')
          toggleButton.textContent = 'Close Editor'
          if (profileNameInput) {
            setTimeout(() => profileNameInput.focus(), 100)
          }
        }

        const closeForm = () => {
          profileForm.classList.add('hidden')
          toggleButton.textContent = 'Edit Profile'
          profileForm.reset()
          statusMessage.classList.add('hidden')
        }

        const showMessage = (text, isSuccess = true) => {
          statusText.textContent = text
          statusMessage.classList.remove('hidden')
          statusMessage.classList.toggle('bg-[#e8f4ea]', isSuccess)
          statusMessage.classList.toggle('border-[#c8d8c2]', isSuccess)
          statusMessage.classList.toggle('bg-[#fff5f5]', !isSuccess)
          statusMessage.classList.toggle('border-[#f0d0cf]', !isSuccess)
          statusText.classList.toggle('text-[#0f4a34]', isSuccess)
          statusText.classList.toggle('text-[#b2534e]', !isSuccess)
        }

        toggleButton.addEventListener('click', function () {
          if (profileForm.classList.contains('hidden')) {
            openForm()
          } else {
            closeForm()
          }
        })

        cancelButton.addEventListener('click', closeForm)

        profileForm.addEventListener('submit', async function (e) {
          e.preventDefault()

          const fullname = document.getElementById('profile-name').value.trim()
          const email = document.getElementById('profile-email').value.trim()
          const password = document.getElementById('profile-password').value

          if (!fullname && !email && !password) {
            showMessage('Tidak ada data yang diubah', false)
            return
          }

          if (email && !email.includes('@')) {
            showMessage('Email tidak valid', false)
            return
          }

          saveButton.disabled = true
          saveBtnText.textContent = 'Saving...'
          saveBtnSpinner.classList.remove('hidden')

          try {
            const formData = new FormData(profileForm)
            const response = await fetch('{{ route("profile.update") }}', {
              method: 'POST',
              body: formData,
            })

            const data = await response.json()

            if (response.ok) {
              showMessage('Profil berhasil diperbarui!', true)
              setTimeout(() => {
                closeForm()
                location.reload()
              }, 1500)
            } else {
              showMessage(data.message || 'Gagal menyimpan profil', false)
            }
          } catch (error) {
            showMessage('Terjadi kesalahan: ' + error.message, false)
          } finally {
            saveButton.disabled = false
            saveBtnText.textContent = 'Save Changes'
            saveBtnSpinner.classList.add('hidden')
          }
        })
      })
    </script>
  @endslot
@end
