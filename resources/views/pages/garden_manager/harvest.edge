@layout.app({ title: "Riwayat Panen | OxyPlant" })
  @slot('main')
    <section class="bg-[#f4f7f1] py-12 md:py-16">
      <div class="mx-auto max-w-6xl px-6">
        <div class="flex flex-wrap items-center justify-between gap-4">
          <div>
            <p class="text-sm font-semibold uppercase tracking-[0.18em] text-[#14573e]">Harvest History</p>
            <h1 class="mt-2 text-3xl font-bold text-[#0A5436] md:text-4xl">Riwayat Panen</h1>
            <p class="mt-3 max-w-2xl text-sm leading-relaxed text-[#4d6056] md:text-base">
              Tanaman yang sudah dipanen tidak dihapus dari sistem. Data disimpan di sini agar Anda tetap bisa melihat riwayatnya.
            </p>
          </div>

          <a
            href="{{ route('garden_manager.index') }}"
            class="inline-flex items-center rounded-full border border-[#b7d0b7] bg-white px-5 py-2.5 text-sm font-semibold text-[#14573e] transition hover:bg-[#eef6ec]"
          >
            Kembali ke Garden Manager
          </a>
        </div>

        <div class="mt-8 grid max-w-sm grid-cols-1 gap-4">
          <div class="rounded-2xl border border-[#dbe3d7] bg-white p-5 shadow-sm">
            <p class="text-xs font-semibold uppercase tracking-[0.16em] text-[#6a7b70]">Total Dipanen</p>
            <p class="mt-3 text-3xl font-bold text-[#0A5436]">{{ totalHarvested }}</p>
          </div>
        </div>
      </div>
    </section>

    <section class="bg-[#cfe0cb] py-12 md:py-16">
      <div class="mx-auto max-w-6xl px-6">
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 xl:grid-cols-3">
          @each(card in harvestCards)
            <article class="overflow-hidden rounded-2xl border border-[#dbe3d7] bg-white p-4 shadow-sm md:p-5">
              <div class="overflow-hidden rounded-xl bg-[#f5f7f3]">
                @if(card.plant.photo_path)
                  <img src="{{ card.plant.photo_path }}" alt="{{ card.plant.name }}" class="aspect-4/3 w-full object-cover" />
                @else
                  <div class="aspect-4/3 flex items-center justify-center text-[#7a8f7a]">
                    <span class="text-sm font-medium">No Image</span>
                  </div>
                @endif
              </div>

              <div class="mt-4">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <h2 class="text-xl font-bold text-[#0A5436]">{{ card.plant.name }}</h2>
                    <p class="mt-1 text-xs uppercase tracking-[0.16em] text-[#6a7b70]">{{ card.plant.type }}</p>
                  </div>
                  <span class="rounded-full bg-[#fff3df] px-3 py-1 text-xs font-semibold text-[#b86512]">
                    Dipanen
                  </span>
                </div>

                <div class="mt-4 grid grid-cols-1 gap-3">
                  <div class="rounded-xl border border-[#e4ebdf] bg-[#fbfdf9] p-3">
                    <p class="text-[11px] font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Tanggal Tanam</p>
                    <p class="mt-1 text-sm font-semibold text-[#14573e]">{{ card.plantedAtLabel }}</p>
                  </div>
                  <div class="rounded-xl border border-[#ffe1ad] bg-[#fff9ee] p-3">
                    <p class="text-[11px] font-semibold uppercase tracking-[0.12em] text-[#b86512]">Tanggal Panen</p>
                    <p class="mt-1 text-sm font-semibold text-[#8f4f0d]">{{ card.harvestedAtLabel }}</p>
                  </div>
                  <div class="grid grid-cols-2 gap-3">
                    <div class="rounded-xl border border-[#e4ebdf] bg-[#fbfdf9] p-3">
                      <p class="text-[11px] font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Disiram</p>
                      <p class="mt-1 text-sm font-semibold text-[#14573e]">{{ card.lastWateringLabel }}</p>
                    </div>
                    <div class="rounded-xl border border-[#e4ebdf] bg-[#fbfdf9] p-3">
                      <p class="text-[11px] font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Dirawat</p>
                      <p class="mt-1 text-sm font-semibold text-[#14573e]">{{ card.lastTreatedLabel }}</p>
                    </div>
                  </div>
                </div>

                @if(card.plant.notes)
                  <div class="mt-4 rounded-xl border border-[#e4ebdf] bg-[#f8faf6] p-3">
                    <p class="text-[11px] font-semibold uppercase tracking-[0.12em] text-[#6a7b70]">Catatan</p>
                    <p class="mt-1 text-sm leading-relaxed text-[#4d6056]">{{ card.plant.notes }}</p>
                  </div>
                @endif
              </div>
            </article>
          @else
            <div class="col-span-full rounded-3xl border-2 border-dashed border-[#b7ccb5] bg-white p-10 text-center shadow-sm md:p-14">
              <div class="mx-auto flex h-20 w-20 items-center justify-center rounded-full bg-[#fff3df] text-[#b86512]">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M5 13l4 4L19 7" />
                </svg>
              </div>
              <h2 class="mt-5 text-2xl font-bold text-[#0A5436]">Belum ada riwayat panen</h2>
              <p class="mx-auto mt-2 max-w-md text-sm leading-relaxed text-[#6a7b70]">
                Tanaman yang Anda konfirmasi panen akan muncul di halaman ini.
              </p>
              <a
                href="{{ route('garden_manager.index') }}"
                class="mt-6 inline-flex items-center rounded-full bg-[#3FAB5E] px-6 py-3 text-sm font-semibold text-white transition hover:bg-[#368F4F]"
              >
                Lihat Tanaman Aktif
              </a>
            </div>
          @endeach
        </div>
      </div>
    </section>
  @endslot
@end
